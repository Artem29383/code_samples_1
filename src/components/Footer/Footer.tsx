import React from 'react';

import * as Styled from './Footer.styled';

const Footer = () => (
  <Styled.Root>
    <Styled.Content>offers simply</Styled.Content>
  </Styled.Root>
);

export default Footer;
